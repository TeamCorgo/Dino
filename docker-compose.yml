services:
  backend:
    build: ./backend
    container_name: fastapi-backend
    ports:
      - "8000:8000"

  frontend:
    build: ./frontend
    container_name: svelte-frontend
    ports:
      - "5173:5173"
    depends_on:
      - backend
